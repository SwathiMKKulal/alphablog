<%= render 'shared/errors', obj: @article %>
<% if logged_in? %>

<%= form_for([@article, @article.comments.build]) do |f| %>
<h6>write your comment about the article!</h6>
	<%= f.text_area :content %>
	<%= f.submit "comment" %>
<% end %>

<% end %>
