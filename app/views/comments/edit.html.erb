<%= render 'shared/errors', obj: @comment %>
<% if logged_in? %>

<%= form_for([@article, @comment]) do |f| %>
	<%= f.text_area :comment %>
	<%= f.submit %>
<% end %>

<% end %>
<%= link_to "back to comment", article_path(@article) %>


